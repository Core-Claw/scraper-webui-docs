---
title: "获取 Worker 任务"
description: "获取 Worker 任务 API 参考"
sidebar:
  order: 53
---

import ApiPlayground from '../../../../../components/ApiPlayground.astro'

**方法:** `GET`

**端点:** `/api/v2/worker-tasks/{workerTaskId}`

**认证:** 支持 `Authorization: Bearer <YOUR_API_KEY>`、`api-key: <YOUR_API_KEY>` 和 `?token=<YOUR_API_KEY>`。推荐优先使用 Bearer token。

## 在线试用

<ApiPlayground method="GET" path="/api/v2/worker-tasks/{workerTaskId}" />

## 什么时候使用这个接口

用于读取某个已保存任务模板的详情。

## 标识符说明

- `workerTaskId` 是已保存任务模板的 slug。

## 路径参数

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `workerTaskId` | 是 | `string` | 已保存 Worker 任务模板的 slug，可从 `GET /api/v2/worker-tasks` 响应的 `data.list[].slug` 获取。 |

## 请求示例

```bash
curl -X GET "https://openapi.coreclaw.com/api/v2/worker-tasks/YOUR_WORKER_TASK_ID" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## 响应示例

```json
{
  "code": 0,
  "data": {
    "id": 1786001196232214890,
    "slug": "01KZAZF168TD9P37RY3RBN6YJ0",
    "worker_id": 32708435341606912,
    "worker_title": "Instagram Profiles By Profile URL",
    "worker_slug": "01KPD6M5YVHWCNQCRK3YQN5WQD",
    "worker_path": "coreclaw/instagram-profile-scraper",
    "version": "v1.0.2",
    "platform_actors_id": 32701213409607680,
    "title": "Instagram Scraper (Task)",
    "input": {
      "parameters": {
        "custom": {
          "profile_url": [{ "url": "https://www.instagram.com/nasa/" }]
        }
      }
    },
    "run_times": 0,
    "last_run_id": 0,
    "last_modified_at": 1782091200,
    "schedule_enabled": 0,
    "schedule_type": 0,
    "created_at": 1782091000,
    "updated_at": 1782091200
  },
  "message": "success",
  "request_id": "req-123"
}
```

## 注意事项

- API v2 同时支持 Bearer token、旧版 `api-key` 请求头和 query token；新集成建议优先使用 Bearer token。

## HTTP 响应

| HTTP 状态 | 应用代码 | 含义 |
| --- | --- | --- |
| `200` | `0` | 请求成功。 |
| `400` | `11000` | 请求参数不合法。 |
| `401` | `12001` | 认证缺失或无效。 |
| `404` | `11004` | 目标资源不存在。 |
| `422` | `11000` | 请求语义或字段校验未通过。 |
| `429` | `13000` | 请求过于频繁。 |
| `500` | `10000` | 服务端内部错误。 |
