---
title: "查询最近一次运行结果"
description: "查询最近一次运行结果 API 参考"
sidebar:
  order: 66
---

import ApiPlayground from '../../../../../components/ApiPlayground.astro'

**方法:** `GET`

**端点:** `/api/v2/worker-runs/last/result`

**认证:** 支持 `Authorization: Bearer <YOUR_API_KEY>`、`api-key: <YOUR_API_KEY>` 和 `?token=<YOUR_API_KEY>`。推荐优先使用 Bearer token。

## 在线试用

<ApiPlayground method="GET" path="/api/v2/worker-runs/last/result" />

## 什么时候使用这个接口

用于分页读取运行结果行，适合结果预览和表格展示。


## 查询参数

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `offset` | 否 | `integer` | 分页偏移量，从 0 开始；用于结果预览、列表翻页或导出前确认数据窗口。 约束：默认 `0`；最小 0。 |
| `limit` | 否 | `integer` | 每页返回数量；列表和结果接口的 `limit` 上限为 `100`。 约束：默认 `20`；范围 1-100。 |

## 请求示例

```bash
curl -X GET "https://openapi.coreclaw.com/api/v2/worker-runs/last/result?offset=0&limit=20" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## 响应示例

```json
{
  "code": 0,
  "data": {
    "count": 1,
    "headers": [
      {
        "key": "title",
        "label": "Title"
      }
    ],
    "list": [
      {
        "title": "Example result"
      }
    ],
    "page_index": 1,
    "page_size": 20
  },
  "message": "success",
  "request_id": "req-123"
}
```

## 注意事项

- API v2 同时支持 Bearer token、旧版 `api-key` 请求头和 query token；新集成建议优先使用 Bearer token。
- 这是账户级最近运行接口，不需要 `workerId` 路径参数；它会作用于当前账户范围内最近一次运行。
- `last` 表示当前筛选范围内最近一次运行；如需确定目标，请使用具体 `runId` 接口。
- 结果列表使用 `offset` 和 `limit` 分页。
- `offset` 从 0 开始；`limit` 默认 `20`，最大 `100`。

## HTTP 响应

| HTTP 状态 | 应用代码 | 含义 |
| --- | --- | --- |
| `200` | `0` | 请求成功。 |
| `400` | `11000` | 请求参数不合法。 |
| `401` | `12001` | 认证缺失或无效。 |
| `404` | `11004` | 目标资源不存在。 |
| `422` | `11000` | 请求语义或字段校验未通过。 |
| `429` | `13000` | 请求过于频繁。 |
| `500` | `10000` | 服务端内部错误。 |
