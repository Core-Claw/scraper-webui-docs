---
title: "List Worker Tasks"
description: "List Worker Tasks API reference"
sidebar:
  order: 50
---

import ApiPlayground from '../../../../components/ApiPlayground.astro'

**Method:** `GET`

**Endpoint:** `/api/v2/worker-tasks`

**Authentication:** Supports `Authorization: Bearer <YOUR_API_KEY>`, `api-key: <YOUR_API_KEY>`, and `?token=<YOUR_API_KEY>`. Prefer Bearer token.

## Try it

<ApiPlayground method="GET" path="/api/v2/worker-tasks" />

## When to use this endpoint

Use this endpoint to list saved Worker task templates.

## Identifier Notes

- `workerId` / `worker_id` accepts a Worker slug or a path encoded as `owner~name` from `owner/name`.

## Query Parameters

| Parameter | Required | Type | Description |
| --- | --- | --- | --- |
| `offset` | No | `integer` | Page number, 1-based. `offset=1` is page 1, `offset=2` is page 2; `offset=0` is accepted as page 1. Out-of-range pages return an empty list. Constraints: default `0`; minimum 0. |
| `limit` | No | `integer` | Page size. `limit` is capped at `100` on list and result endpoints. Constraints: default `20`; range 1-100. |
| `worker_id` | No | `string` | Worker slug or path. You may paste `owner/name`; the playground sends it as `owner~name` for query values. |
| `keyword` | No | `string` | Keyword for task title or slug |

## Request Example

```bash
curl -X GET "https://openapi.coreclaw.com/api/v2/worker-tasks?offset=0&limit=20&worker_id=YOUR_WORKER_ID&keyword=coffee" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Response Example

```json
{
  "code": 0,
  "data": {
    "count": 1,
    "list": [
      {
        "slug": "01KX4ZM5DE8Y8GAZQ7R0KYZ3FD",
        "worker_title": "Google Maps Scraper",
        "worker_slug": "01KPD6M5YQADCQKGVKPDZVYC63",
        "worker_path": "coreclaw/google-maps-scraper",
        "version": "v1.2.6",
        "platform_actors_id": 32684270703149056,
        "title": "Google Maps Scraper (Task)",
        "description": "Scrape Google Maps business records on a schedule.",
        "input": {
          "parameters": {
            "custom": {
              "keywords": [{ "keyword": "coffee" }],
              "max_results": 10
            }
          }
        },
        "run_times": 3,
        "last_run_id": 0,
        "last_modified_at": 1782091200,
        "schedule_enabled": 0,
        "schedule_type": 0,
        "created_at": 1782000000,
        "updated_at": 1782091200
      }
    ],
    "page_index": 1,
    "page_size": 20
  },
  "message": "success",
  "request_id": "req-123"
}
```

## Notes

- API v2 supports Bearer token, the legacy `api-key` header, and query token. Prefer Bearer token for new integrations.

## HTTP Responses

| HTTP Status | Application Code | Meaning |
| --- | --- | --- |
| `200` | `0` | OK |
| `400` | `11000` | Bad Request |
| `401` | `12001` | Unauthorized |
| `404` | `11004` | Not Found |
| `422` | `11000` | Unprocessable Entity |
| `429` | `13000` | Too Many Requests |
| `500` | `10000` | Internal Server Error |
